

# plot_lextrema <- function(lextrema){
#   stopifnot(any(class(lextrema)=="lextrema"),
#             any(class(lextrema$model)=="lextrema"))
#
#   plot <- marginaleffects::plot_predictions(test_compr$model, by="x", conf_level=0.96)
#
#   #x and y -dependent
#   plot <- plot+
#     geom_point(aes(x = test_compr$model$model$x, y = test_compr$model$model$y))
#
#
# }













